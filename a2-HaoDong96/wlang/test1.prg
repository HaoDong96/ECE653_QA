havoc x, y, z;
while (x > 1 and y < 1) or z = 1 do {
    x := x - 1;
    y := y + 1;
    z := z * -1;
    while (x > 10 and y < 1000) or z = 100 do {
        x := x - 1;
        y := y + 1;
        z := z * -1
    }
}